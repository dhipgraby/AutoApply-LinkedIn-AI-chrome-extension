const matchJobList = (jobList) => {
    return `
    From this object return just the indexes in a js array, for the job titles think could be a good match base on previews CV
    ${jobList}    
    Reply only into js array format with indexes like [1,3,4]. Not more text or further explanation`
} 
